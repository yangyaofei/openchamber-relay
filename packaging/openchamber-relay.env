# Environment for the openchamber-relay system service (deb/rpm installs).
# Edit and restart: sudo systemctl restart openchamber-relay

PORT=8080
RELAY_VERIFY_AUTH=false
# RELAY_SERVICE_NAME=openchamber-relay
